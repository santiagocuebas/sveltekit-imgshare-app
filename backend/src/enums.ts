export enum UserRole {
  EDITOR = 'editor',
  MOD = 'mod',
  ADMIN = 'admin',
  SUPER = 'superadmin'
}

export enum Score {
  LIKE = 'like',
  DISLIKE = 'dislike'
}

export enum OrderGallery {
  NEWEST = 'NEWEST',
  OLDEST = 'OLDEST',
  VIEWED = 'VIEWED'
}

export enum ShowValues {
  PUBLIC = 'PUBLIC',
  PRIVATE = 'PRIVATE'
}

export enum Ext {
  PNG = '.png',
  JPG = '.jpg',
  JPEG = '.jpeg',
  GIF = '.gif'
}
