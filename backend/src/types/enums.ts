export enum UserRole {
	EDITOR = 'editor',
	MOD = 'mod',
	ADMIN = 'admin',
	SUPER = 'superadmin',
}

export enum Score {
	LIKE = 'likes',
	DISLIKE = 'dislikes',
}

export enum Folder {
	USER = 'uploads/avatar/',
	PUBLIC = 'uploads/',
}

export enum OrderGallery {
	NEWEST = 'NEWEST',
	OLDEST = 'OLDEST',
	VIEWED = 'VIEWED',
}

export enum ShowValues {
	PUBLIC = 'PUBLIC',
	PRIVATE = 'PRIVATE',
}
